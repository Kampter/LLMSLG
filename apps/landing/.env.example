# Public base URL of apps/server (the authoritative game backend).
# Used at runtime by app/components/ChatPanel.tsx and ResourceDisplay.tsx.
#
# Leave unset on Vercel until apps/server has a cloud deployment. The
# client falls back to http://localhost:8000 in local development.
NEXT_PUBLIC_API_URL=
